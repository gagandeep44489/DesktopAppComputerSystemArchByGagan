import { Card, CardContent, CardMedia, Container, Grid, List, ListItem, Typography } from '@mui/material';
import { SectionTitle } from '@/presentation/components/common/SectionTitle';
import { LoadingState } from '@/presentation/components/feedback/StateViews';
import { useCoach } from '@/presentation/features/coach/hooks';
export default function CoachPage() { const { data, isLoading } = useCoach(); if (isLoading) return <LoadingState />; return <Container sx={{ py: 6 }}><SectionTitle eyebrow="About" title={data?.name ?? 'Coach'} subtitle={data?.title} /><Grid container spacing={4}><Grid size={{ xs: 12, md: 5 }}><CardMedia component="img" image={data?.imageUrl} alt={data?.name} sx={{ borderRadius: 4 }} /></Grid><Grid size={{ xs: 12, md: 7 }}><Card><CardContent><Typography>{data?.bio}</Typography><Typography variant="h5" sx={{ mt: 3 }}>Experience</Typography><Typography>{data?.experienceYears} years coaching and playing competitive cricket.</Typography><Typography variant="h5" sx={{ mt: 3 }}>Achievements</Typography><List>{data?.achievements.map((x) => <ListItem key={x}>{x}</ListItem>)}</List><Typography variant="h5">Certifications</Typography><List>{data?.certifications.map((x) => <ListItem key={x}>{x}</ListItem>)}</List><Typography variant="h5">Playing Career</Typography><Typography>{data?.playingCareer}</Typography><Typography variant="h5" sx={{ mt: 2 }}>Coaching Career</Typography><Typography>{data?.coachingCareer}</Typography></CardContent></Card></Grid></Grid></Container>; }
