import { Box, Button, Card, CardContent, CardMedia, Container, Grid, Stack, Typography } from '@mui/material';
import { Link } from 'react-router-dom';
import { SectionTitle } from '@/presentation/components/common/SectionTitle';
import { LoadingState } from '@/presentation/components/feedback/StateViews';
import { useCoach, usePrograms, useTestimonials } from '@/presentation/features/home/hooks/useQueries';
export default function HomePage() { const coach = useCoach(); const programs = usePrograms(); const testimonials = useTestimonials(); if (coach.isLoading) return <LoadingState />; return <Box><Box sx={{ bgcolor: 'primary.main', color: 'white', py: { xs: 8, md: 12 } }}><Container><Grid container spacing={5} alignItems="center"><Grid size={{ xs: 12, md: 6 }}><Typography variant="h1">Train smarter. Play braver.</Typography><Typography sx={{ my: 3 }} variant="h5">Modern cricket coaching for kids, beginners, academy players, and elite performers.</Typography><Stack direction="row" spacing={2}><Button component={Link} to="/booking" variant="contained" color="secondary">Book Session</Button><Button component={Link} to="/training" variant="outlined" color="inherit">Learn More</Button></Stack></Grid><Grid size={{ xs: 12, md: 6 }}><CardMedia component="img" image={coach.data?.imageUrl} alt="Professional cricket coach" sx={{ borderRadius: 4, maxHeight: 420 }} /></Grid></Grid></Container></Box><Container sx={{ py: 8 }}><SectionTitle eyebrow="Programs" title="Coaching pathways" subtitle="Structured plans with measurable outcomes." /><Grid container spacing={3}>{programs.data?.slice(0, 3).map((program) => <Grid key={program.id} size={{ xs: 12, md: 4 }}><Card><CardContent><Typography variant="h5">{program.title}</Typography><Typography color="text.secondary">{program.description}</Typography></CardContent></Card></Grid>)}</Grid><SectionTitle eyebrow="Reviews" title="Trusted by players and parents" />{testimonials.data?.map((item) => <Typography key={item.id} sx={{ my: 1 }}>“{item.quote}” — {item.author}</Typography>)}</Container></Box>; }
