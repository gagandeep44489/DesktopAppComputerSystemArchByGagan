# Deployment

Build with `npm run build` and deploy the generated `dist` directory to any static host such as Netlify, Vercel, Cloudflare Pages, S3, or Azure Static Web Apps. Configure `VITE_API_BASE_URL` in the deployment environment.
